#!/bin/sh

post_install() {
	echo ":: To immediately launch the fishcake tray icon, run: 'systemctl --user start fishcake-trayd.service'"
	echo ":: If using gnome-shell, make sure to install and enable the 'AppIndicator and KStatusNotification Support' extension!"
}
