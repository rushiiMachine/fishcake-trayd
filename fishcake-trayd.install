#!/bin/sh

post_install() {
	echo ":: To enable and autolaunch the fishcake tray icon, run: 'systemctl --user enable --now fishcake-trayd.service'"
	echo ":: If using gnome-shell, make sure to install and enable the 'AppIndicator and KStatusNotification Support' extension!"
}
